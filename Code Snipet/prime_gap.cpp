//maximum prime gap can be (logn)^2
//check a number prime or not in sqrt(n);
//so by using monkey way total time complexity will be 
    /*
        =sqrt(n)*(log(n))^2
        for n=1e9,
        =32000*(30)^2
    */