#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,q;
    cin>>n>>q;
    int a[n+1];
    for(int i=1;i<=n;i++)
    {
        cin>>a[i];
    }

    // n=10^5
    // q=10^5

    // 100
    // O(n^2)
    // Worst

    // O(nlogn);
    // 
    for(int i=1;i<=q;i++){
        // int in;
        // cin>>in;
        // int cnt=0;
        // for(int j=1;j<=n;j++){
        //     if(a[j]>=a[in] && a[j]>0) cnt++;
        // }
        // cout<<cnt<<endl;
    }
}